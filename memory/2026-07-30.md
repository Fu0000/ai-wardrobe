# 2026-07-30

- 微信小程序正式 AppID 已写入 `miniapp/src/manifest.json`；AppSecret 只保存在 Git 忽略、权限为 `0600` 的根目录 `.env`，未记录具体值。
- 本地小程序 API 地址已写入 Git 忽略的 `miniapp/.env.local`。
- 使用合成一次性 code 验证真实 API → 微信 `code2Session`：微信接受 AppID/AppSecret，并按预期返回无效 code；API 映射为 `401 WECHAT_CODE_INVALID`。
- 新增成功登录 HTTP + PostgreSQL 集成测试，覆盖身份加密落库、Token、`/me`、重复登录复用用户和登录成功事件。
- Docker Desktop 因宿主机空间不足进入只读/无法启动状态；只清理了可重建的 NPM、pnpm 包缓存，未删除 Docker Volume 或用户数据，随后恢复 PostgreSQL 18/pgvector、Redis、Migration 和 API。
- 验证结果：后端 `377/377`、小程序 `73/73`，Lint、Mypy/Vue Typecheck、生产构建与供应链审计均通过；已跟踪文件中未发现 AppSecret。
- 提交已推送：`8519542`（登录成功链路集成测试）、`1cca113`（小程序 AppID）。
- 已安装并签名校验微信开发者工具稳定版，用户扫码后完成真实 `wx.login`：登录接口返回 `200`，落库 1 个用户、1 个加密微信身份和成功事件；重复真实登录后用户/身份数仍为 1，证明幂等复用生效。
- 真实 Token 从小程序本地认证状态调用 `/api/v1/me` 返回 `200`，MCP 截图确认首页正常渲染且 Console 无 error/warn。
- 修复 HTTPX/HTTPCore INFO 日志会记录完整第三方请求 URL 的风险，回归验证敏感微信查询参数不再进入应用日志；提交 `fe39b2b` 已推送。
- 已将 `weixin-devtools-mcp@0.5.1` 配置为用户级 Codex MCP。实测其连接、截图、Console、AppService 和网络列表可用；增强 Network 拦截会影响当前 uni-app 的 `wx.request` 回调，功能验收必须先停止网络监听，元素快照/选择器阻塞则使用截图、脚本和后端日志替代。
- 本地 COS 降级已实现为私有磁盘对象存储，保留签名 PUT/GET、路径隔离、原子写入、资产校验和 Readiness 语义；只允许 `local`/`test`，生产环境仍强制真实 COS。
- 本地确定性 Diagnosis、Image Edit、Critic 适配器已接入；图片路由保持精确模型 `gpt-image-2-2026-04-21` 优先，远程不可用时才本地回退，数据库记录每次实际 Provider/Model。
- 同一微信用户完成授权、上传、诊断、优化、分享、投票和删除清理全链路；最终冒烟 `PASSED`、清理 `COMPLETED`，诊断页与 Before/After 页完成开发者工具视觉验收。
- 当前 OpenAI-compatible 端点尚未映射精确图片模型，真实请求记录为 `FAILED/UNAVAILABLE`；本地图片适配器第 2 路成功，只能作为产品和故障路径演示，不能作为真实模型质量证据。
- 本地冒烟脚本已绕过 localhost 的系统代理，避免环境代理把本地请求转换为空 `503`。
- 修复微信图片上传误报“无法读取所选图片”：开发者工具/uni-app 可能返回跨 Realm
  `ArrayBuffer` 或 `Uint8Array`，不能只用 `instanceof ArrayBuffer` 判断。生产构建已在
  微信真实文件系统完成 Upload Ticket → PUT → Complete，资产校验为 `READY`，随后发起删除。
